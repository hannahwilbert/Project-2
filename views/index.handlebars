	<div class="jumbotron">
		<div class="container">
		<div class="row head">
			<div class="col-xs-12">
			  <h1 class="display-3 text-center" style="font-weight: 900; font-size: 3em;">GigEquipt</h1>
			  <hr class="my-4">
			  <p class="lead">Simple application that performs CRUD operations to a mySQL database via JawsDB deployed on heroku.</p>
			  <p class="lead">Retrieves data from the database (Needed Equiptment), updates equiptment value when packed, creates equiptment list (Add equiptment), &amp; deletes all data from the database (Delete button) using the ORM Sequelize.</p>
			</div>
		</div>
		</div>
	</div>
<div class="container">
	<div class="row">
		<div class="col-xs-6 text-center" class="task">
			<div class="panel panel-default">
				<div class="panel-heading" style="font-weight: 900; font-size: 1.5em;">Needed Equiptment</div>
				<div class="panel-body">
					{{#each equiptment_data}}
						{{#unless this.packed}}
							<div class="row">
								<div class="col-xs-9 text-left" style="font-weight: bold;">
									<pre style="font-size: 1.25em;"> {{this.id}}. {{this.equiptment_name}}</pre>
								</div>
								<div class="col-xs-3 text-center">
									<form method="POST" action="equiptment/{{id}}" class="button-size" style="line-height: 0px;">
										<input type="hidden" name="equiptment_id" type="text" value={{this.id}}>
										<br>
										<button type="sumbit" class="btn btn-default btn-success">Pack it!</button>
									</form>
								</div>
							</div>
						{{/unless}}
					{{/each}}
				</div>
			</div>
		</div>
		<div class="col-xs-6 text-center" class="task">
			<div class="panel panel-default">
				<div class="panel-heading" style="font-weight: 900; font-size: 1.5em;">Packed Equiptment</div>
				<div class="panel-body">
					{{#each equiptment_data}}
						{{#if this.packed}}
							<input class="form-control" type="text" placeholder="{{this.id}}. {{this.equiptment_name}}" readonly>
						{{/if}}
					{{/each}}
				</div>
			</div>
		</div>
	</div>
	<br>
	<div class="row">
		<div class="col-xs-6 text-center enter_section">
			<div class="panel panel-default">
				<div class="panel-heading" style="font-weight: 900; font-size: 1.5em;">What equiptment do you need?</div>
				<div class="panel-body">
					<form action="/add" method="POST">
						<div class="input-group">
							<input type="text" name="equiptment_name" class="form-control" value="" id="enter_text">
							<br>
							<span class="input-group-btn">
								<button type="sumbit" class="btn btn-primary" id="text-enter-button">Add</button>
							</span>
						</div>
					</form>
				</div>
			</div>
		</div>
		<div class="col-xs-6 text-center enter_section">
			<div class="panel panel-default">
				<div class="panel-heading" style="font-weight: 900; font-size: 1.5em;">Clear all Equiptment!</div>
				<div class="panel-body">
					<form action="/delete" method="POST">
						<button type="sumbit" class="btn btn-danger" id="text-enter-button">Delete</button>
					</form>
				</div>
			</div>
		</div>
	</div>
</div>